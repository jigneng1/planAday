import 'package:flutter/material.dart';

class EditPlanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit plan', style: TextStyle(fontSize: 24)),
    );
  }
}
